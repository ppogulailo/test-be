# Copy to .env and set values for docker compose
# cp env.example .env

# POSTGRES_USER=user
# POSTGRES_PASSWORD=password
# POSTGRES_DB=db
# # Host port for Postgres (default 5433 if 5432 is in use by local Postgres)
# POSTGRES_PORT=5433

JWT_SECRET=change-me-in-production

PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
export DATABASE_URL="postgresql://deveteria:deveteria@localhost:5432/deveteria?schema=public"
